import styled from 'styled-components';

export const BannerWrapper = styled.section`
  background: #aab98e;
  margin: 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 2.5rem 0;

  ul {
    list-style: none;

    li {
      font-size: 1.25rem;
      color: #fff;
      text-align: left;
    }
  }
`;
